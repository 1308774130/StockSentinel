version: '3.8'

services:
  stock-bot:
    build: .
    container_name: stock-bot
    restart: always
    environment:
      - FEISHU_WEBHOOK=${FEISHU_WEBHOOK}
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
      - ./stock_monitor.db:/app/stock_monitor.db
